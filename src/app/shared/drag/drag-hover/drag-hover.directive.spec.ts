import {DragHoverDirective} from './drag-hover.directive';

describe(DragHoverDirective.name, () => {
    it('should create an instance', () => {
        // const directive = new DragOverDocumentDirective();
        // expect(directive).toBeTruthy();
    });
});
