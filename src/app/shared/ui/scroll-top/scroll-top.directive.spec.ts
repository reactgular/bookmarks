import {ScrollTopDirective} from './scroll-top.directive';

describe(ScrollTopDirective.name, () => {
    it('should create an instance', () => {
        const directive = new ScrollTopDirective();
        expect(directive).toBeTruthy();
    });
});
